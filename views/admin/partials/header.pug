header.header
  .header-container
    .header-left
      .logo
        a(href="/")
          h1 miniStack
    .header-right
      .admin-profile
        a.profile-link(href="/admin/profile")
          if user && user.Avatar
            img.rounded-circle.me-2(src=user.Avatar, alt="Avatar", width="32", height="32")
          else
            img(src="https://e7.pngegg.com/pngimages/349/288/png-clipart-teacher-education-student-course-school-avatar-child-face.png" alt="Admin" width="32" height="32")
          if user
            span= user.name
            
      .auth-buttons
        a.btn.btn-logout(href="/admin/auth/logout") Đăng xuất
