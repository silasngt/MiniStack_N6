header.header
  .header-container
    .header-left
      .logo
        a(href="/")
          h1 miniStack
    .header-right
      .admin-profile
        a.profile-link(href="/admin/profile")
          if currentUser && currentUser.avatar
              img.rounded-circle.me-2(src=currentUser.avatar, alt="Avatar", width="32", height="32")
          else
              img.rounded-circle.me-2(src="/images/default-avatar.png", alt="Avatar", width="32", height="32")
          if currentUser
              span= currentUser.name
            
      .auth-buttons
        a.btn.btn-logout(href="/admin/auth/logout") Đăng xuất
