extends ../../layouts/default.pug


block main
  h1 Trang chủ đề
  each item in user
      h1 #{item.userid}
      h1 #{item.fullname}
      h1 #{item.email}
      h1 #{item.password}
      h1 #{item.gender}
      h1 #{item.role}